package NetWork;

/**
 * @author ScholarChen20
 * @version 0.1
 */
@SuppressWarnings({"all"})
public class NetStat {
    /**
     * 1.netstat -an 显示当前主机网络情况，包括端口监听情况和网络连接情况
     * 2.讷tstat -an | more 可以分页显示
     * 3.Listening 表示某个端口在监听
     * 4. 如果一个外部程序连接到端口，就会显示一条连接信息
     *    netstat -anb
     */
}
